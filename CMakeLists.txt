cmake_minimum_required(VERSION 2.8.8)

project(clang-tags)

set(ClangTags_VERSION_MAJOR 0)
set(ClangTags_VERSION_MINOR 1)
set(ClangTags_VERSION_PATCH 0)

enable_testing()

########################################################################

add_subdirectory(src)

### CMakeLists.txt ends here
